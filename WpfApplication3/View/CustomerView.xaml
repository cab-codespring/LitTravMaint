<UserControl x:Class="LitTravProj.View.CustomerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="300" d:DesignWidth="453">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition />
            <RowDefinition Height="Auto"/>
            <RowDefinition />
        </Grid.RowDefinitions>

        <Grid Name="gridTopRow" Grid.Row="0" Background="Beige">
            <Label Content="Company Name" Height="28" HorizontalAlignment="Left" Margin="114,50,0,0" Name="label1" VerticalAlignment="Top" />
            <TextBox Name="CompanyNameTB1" Text="{Binding CompanyName}" Height="23" HorizontalAlignment="Left" Margin="264,52,0,0" VerticalAlignment="Top" Width="120" />
        </Grid>
        
        <GridSplitter 
            ResizeDirection="Rows" 
            Grid.Column="0" 
            Grid.ColumnSpan="2" 
            Grid.Row="1" 
            Width="Auto" 
            Height="3" 
            HorizontalAlignment="Stretch" 
            VerticalAlignment="Stretch" 
            Margin="0"/>
            
            <Grid Grid.Row="2" Background="Gray">
            <DataGrid  Name="dataGrid1"  Background="Beige" AutoGenerateColumns="True"  HorizontalAlignment="Stretch"  Margin="12,12,0,0" VerticalAlignment="Stretch"
                 ItemsSource="{Binding Customers}" />
        </Grid>
    </Grid>
</UserControl>
